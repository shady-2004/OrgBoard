# صفحة السعودة - تتبع رخص العمل والإبعاد

## نظرة عامة
صفحة السعودة مخصصة لتتبع حالة رخص العمل والإبعاد للموظفين في المنظمات المختلفة.

## الميزات

### 📋 صفحة القائمة الرئيسية (`SaudizationPage`)
- **عرض جدول شامل** يحتوي على:
  - المنظمة
  - اسم الموظف
  - التاريخ
  - حالة رخصة العمل (قيد الانتظار / مشكلة في الإصدار / تم الإصدار)
  - حالة الإبعاد (قيد الانتظار / تم الإبعاد)
  - تاريخ الإبعاد (إن وجد)
  
- **البحث**: إمكانية البحث باسم الموظف
- **Pagination**: دعم الصفحات المتعددة
- **حالات ملونة**: كل حالة لها لون مميز:
  - رخصة العمل:
    - قيد الانتظار: أصفر
    - مشكلة في الإصدار: أحمر
    - تم الإصدار: أخضر
  - الإبعاد:
    - قيد الانتظار: أزرق
    - تم الإبعاد: أحمر

### ➕ صفحة الإضافة (`AddSaudizationPage`)
- اختيار المنظمة من قائمة منسدلة
- إدخال اسم الموظف
- اختيار التاريخ (لا يمكن اختيار تاريخ في المستقبل)
- اختيار حالة رخصة العمل
- اختيار حالة الإبعاد
- **تاريخ الإبعاد**: يظهر تلقائياً فقط عند اختيار "تم الإبعاد" (حقل إجباري)
- ملاحظات (اختياري، حتى 500 حرف)

### ✏️ صفحة التعديل (`EditSaudizationPage`)
- نفس حقول صفحة الإضافة
- تعبئة البيانات الحالية تلقائياً
- إمكانية تعديل جميع الحقول

## البنية التقنية

### Backend Model
```typescript
{
  organization: ObjectId,      // المؤسسة
  date: Date,                   // التاريخ
  employeeName: string,         // اسم الموظف
  workPermitStatus: enum,       // حالة رخصة العمل
  deportationStatus: enum,      // حالة الإبعاد
  deportationDate?: Date,       // تاريخ الإبعاد (اختياري)
  notes?: string               // ملاحظات
}
```

### API Endpoints
```
GET    /api/saudization              - جلب جميع السجلات (مع pagination وبحث)
GET    /api/saudization/:id          - جلب سجل واحد
POST   /api/saudization              - إنشاء سجل جديد
PATCH  /api/saudization/:id          - تحديث سجل
DELETE /api/saudization/:id          - حذف سجل
```

### Frontend Routes
```
/saudization                 - القائمة الرئيسية
/saudization/add            - إضافة سجل جديد
/saudization/edit/:id       - تعديل سجل موجود
```

## مثال على الاستخدام

1. **إضافة سجل جديد**:
   - انتقل إلى `/saudization`
   - اضغط على "إضافة سجل جديد"
   - املأ البيانات المطلوبة
   - إذا كانت حالة الإبعاد "تم الإبعاد"، سيظهر حقل تاريخ الإبعاد
   - اضغط "حفظ"

2. **البحث عن موظف**:
   - في صفحة القائمة، استخدم مربع البحث
   - اكتب اسم الموظف
   - ستظهر النتائج تلقائياً

3. **تعديل سجل**:
   - من القائمة، اضغط "تعديل" بجانب السجل المطلوب
   - عدّل البيانات
   - اضغط "حفظ التعديلات"

4. **حذف سجل**:
   - من القائمة، اضغط "حذف"
   - أكد عملية الحذف

## Validation Rules

- **اسم الموظف**: 
  - إجباري
  - بين 2-100 حرف
  - يجب أن يحتوي على أحرف فقط

- **التاريخ**: 
  - إجباري
  - لا يمكن أن يكون في المستقبل

- **تاريخ الإبعاد**:
  - إجباري فقط عند اختيار "تم الإبعاد"
  - لا يمكن أن يكون في المستقبل

- **الملاحظات**: 
  - اختياري
  - حد أقصى 500 حرف

## الفرق عن النسخة السابقة

هذه النسخة تتعامل مع:
- ✅ تتبع حالة رخص العمل للموظفين
- ✅ تتبع حالة الإبعاد
- ✅ ربط بمنظمة محددة
- ✅ معلومات على مستوى الموظف الفردي

النسخة السابقة كانت:
- ❌ تتبع نسب السعودة على مستوى ربع سنوي
- ❌ حسابات إجمالية فقط
